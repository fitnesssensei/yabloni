# 🌳 Яблони и Груши - Интернет-магазин саженцев

Django-based e-commerce платформа для продажи саженцев плодовых деревьев (яблонь и груш) с полным функционалом интернет-магазина.

## 📋 Обзор проекта

Проект представляет собой современный интернет-магазин с каталогом товаров, корзиной покупок, системой заказов и блогом. Разработан с использованием фреймворка Django 5.2.7 с адаптивным дизайном на Bootstrap 5.

## 🛠 Технологический стек

### Backend
- **Django 5.2.7** - основной веб-фреймворк
- **PostgreSQL** - база данных (psycopg2-binary драйвер)
- **Python-dotenv** - управление переменными окружения
- **Pillow** - обработка изображений

### Frontend
- **Bootstrap 5** - CSS-фреймворк для адаптивного дизайна
- **Bootstrap Icons** - библиотека иконок
- **Font Awesome** - дополнительная библиотека иконок
- **jQuery** - JavaScript библиотека для DOM и AJAX

### Django-приложения
- **django-crispy-forms** с шаблоном **crispy-bootstrap5** - стилизация форм

## 🏗 Архитектура проекта

### Структура приложений

```
yabloni/
├── catalog/          # Каталог товаров
├── cart/             # Корзина покупок
├── orders/           # Управление заказами
├── blog/             # Блог со статьями
├── config/           # Основные настройки Django
├── templates/        # HTML шаблоны
├── static/           # Статические файлы (CSS, JS, изображения)
├── media/            # Медиафайлы (загруженные изображения)
└── venv/             # Виртуальное окружение
```

### Модели данных

#### Приложение `catalog`
- **Category** - категории товаров
  - `name` - название категории
  - `slug` - URL-идентификатор
- **Product** - товары
  - `name` - название товара
  - `slug` - URL-идентификатор
  - `category` - связь с категорией
  - `price` - цена
  - `description` - описание
  - `image`, `main_image` - изображения
  - `available` - наличие
  - `is_new` - флаг новинки
  - `is_featured` - флаг хита продаж

#### Приложение `blog`
- **BlogPost** - статьи блога
  - `title` - заголовок
  - `slug` - URL-идентификатор
  - `content` - содержание
  - `image` - изображение
  - `is_published` - флаг публикации

#### Приложение `cart`
- **CartItem** - элементы корзины
- **Cart** - корзина (основана на сессиях)

#### Приложение `orders`
- **Order** - заказы
- **OrderItem** - элементы заказа

## 🚀 Функционал

### Основные возможности
- **Каталог товаров** с фильтрацией по категориям
- **Поиск товаров** по названию, описанию и категории
- **Корзина покупок** на основе сессий
- **Оформление заказов** с email-уведомлениями
- **Регистрация и аутентификация** пользователей
- **Блог** с публикацией статей
- **Адаптивный дизайн** для мобильных устройств

### Особенности
- **Хиты продаж** и **новинки** на главной странице
- **Модальное окно поиска** с автодополнением
- **Счетчик товаров** в корзине
- **Русскоязычный интерфейс**
- **SEO-дружественные URL** (slug-based)

## 📦 Установка и настройка

### Требования
- Python 3.8+
- PostgreSQL 12+
- pip

### Шаги установки

1. **Клонирование репозитория**
   ```bash
   git clone <repository-url>
   cd yabloni
   ```

2. **Активация виртуального окружения**
   ```bash
   source venv/bin/activate
   ```

3. **Установка зависимостей**
   ```bash
   pip install -r requirements.txt
   ```

4. **Настройка базы данных**
   - Создать базу данных PostgreSQL
   - Настроить параметры в файле `.env`:
   ```env
   DB_NAME=yabloni_db
   DB_USER=your_username
   DB_PASSWORD=your_password
   DB_HOST=localhost
   DB_PORT=5432
   ```

5. **Миграции базы данных**
   ```bash
   python manage.py makemigrations
   python manage.py migrate
   ```

6. **Сбор статических файлов**
   ```bash
   python manage.py collectstatic
   ```

7. **Создание суперпользователя**
   ```bash
   python manage.py createsuperuser
   ```

8. **Запуск сервера**
   ```bash
   python manage.py runserver
   ```

## ⚙️ Конфигурация

### Основные настройки (config/settings.py)
- **LANGUAGE_CODE**: 'ru-ru' - русский язык
- **TIME_ZONE**: 'Europe/Moscow' - московское время
- **MEDIA_URL/MEDIA_ROOT**: настройки медиафайлов
- **STATIC_URL/STATIC_ROOT**: настройки статических файлов
- **EMAIL_***: настройки для email-уведомлений (Gmail SMTP)

### URL-маршруты
- `/` - главная страница
- `/catalog/` - каталог товаров
- `/cart/` - корзина
- `/orders/` - заказы
- `/blog/` - блог
- `/login/`, `/register/` - аутентификация

## 🎨 Дизайн и шаблоны

### Структура шаблонов
```
templates/
├── base/              # Базовые шаблоны
│   ├── header.html    # Шапка сайта
│   ├── footer.html    # Подвал
│   └── base.html      # Основной шаблон
├── catalog/           # Шаблоны каталога
├── cart/              # Шаблоны корзины
├── orders/            # Шаблоны заказов
├── blog/              # Шаблоны блога
└── registration/      # Шаблоны аутентификации
```

### CSS-фреймворки
- **Bootstrap 5** для адаптивного дизайна
- **Кастомные стили** в `static/css/`
- **Иконки** Bootstrap Icons и Font Awesome

## 📧 Email-настройки

Проект использует Gmail SMTP для отправки уведомлений:
- **EMAIL_HOST**: smtp.gmail.com
- **EMAIL_PORT**: 587
- **EMAIL_USE_TLS**: True
- **EMAIL_HOST_USER**: ваш@gmail.com
- **EMAIL_HOST_PASSWORD**: пароль приложения Gmail

## 🔧 Разработка

### Команды управления
```bash
# Запуск сервера разработки
python manage.py runserver

# Создание миграций
python manage.py makemigrations

# Применение миграций
python manage.py migrate

# Сбор статических файлов
python manage.py collectstatic

# Создание суперпользователя
python manage.py createsuperuser

# Запуск shell
python manage.py shell
```

### Тоннель для внешнего доступа
```bash
# Во втором терминале после запуска сервера
lt --port 8000
```

## 📝 TODO и улучшения

### Планируемые улучшения
- [ ] Добавление системы отзывов о товарах
- [ ] Интеграция с платежными системами
- [ ] Панель администратора для управления заказами
- [ ] Система скидок и промокодов
- [ ] Мультиязычность
- [ ] Оптимизация SEO
- [ ] Кэширование
- [ ] Unit-тесты

### Известные проблемы
- Дублирование функции поиска в `catalog/views.py`
- Отсутствует пароль БД в `.env` файле
- Дублированный модальный код в `templates/base/header.html`
- Отсутствует функция отправки email-уведомлений

## 📄 Лицензия


## 👥 Авторы

Разработано с использованием Django и современных веб-технологий.

---

**Примечание**: Для корректной работы проекта необходимо настроить базу данных PostgreSQL и заполнить файл `.env` соответствующими параметрами. Обязательно оставляйте много комментариев на русском языке.
